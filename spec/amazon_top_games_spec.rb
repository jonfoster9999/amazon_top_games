require "spec_helper"

describe AmazonTopGames do
  it "has a version number" do
    expect(AmazonTopGames::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
